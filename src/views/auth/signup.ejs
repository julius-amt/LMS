<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Library Management System - Signup</title>
        <script src="https://cdn.tailwindcss.com"></script>
    </head>
    <body class="bg-gray-100 flex items-center justify-center min-h-screen">
        <div class="bg-white p-8 rounded-lg shadow-md w-96">
            <h2 class="text-center text-3xl font-bold mb-6 text-[#228B22]">
                Sign Up
            </h2>
            <form action="/auth/signup" method="POST">
                <div class="mb-4">
                    <label for="name" class="block text-[#8A9A5B] mb-2"
                        >Full Name</label
                    >
                    <input
                        type="text"
                        id="name"
                        name="name"
                        required
                        class="w-full px-3 py-2 border border-[#8A9A5B] rounded-md focus:outline-none focus:ring-2 focus:ring-[#228B22]"
                    />
                </div>

                <div class="mb-4">
                    <label for="email" class="block text-[#8A9A5B] mb-2"
                        >Email Address</label
                    >
                    <input
                        type="email"
                        id="email"
                        name="email"
                        required
                        class="w-full px-3 py-2 border border-[#8A9A5B] rounded-md focus:outline-none focus:ring-2 focus:ring-[#228B22]"
                    />
                </div>

                <div class="mb-6">
                    <label for="password" class="block text-[#8A9A5B] mb-2"
                        >Password</label
                    >
                    <input
                        type="password"
                        id="password"
                        name="password"
                        required
                        class="w-full px-3 py-2 border border-[#8A9A5B] rounded-md focus:outline-none focus:ring-2 focus:ring-[#228B22]"
                    />
                </div>

                <button
                    type="submit"
                    class="w-full bg-[#228B22] text-white py-2 rounded-md hover:bg-[#8A9A5B] transition duration-300"
                >
                    Create Account
                </button>
            </form>

            <p class="text-center mt-4 text-sm text-gray-600">
                Already have an account?
                <a href="/auth/login" class="text-[#228B22] hover:underline"
                    >Log In</a
                >
            </p>
        </div>
        <script>
            const error = "<%= error %>";
            if (error) {
                alert(error);
            }
        </script>
    </body>
</html>
